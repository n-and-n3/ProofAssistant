{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - アクティブファイルのデバッグ",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "stopOnEntry": false,
            "environment": [],
            "preLaunchTask": "g++ build active file"
        }
    ]
}